; The example inputs, shared by law_interp, law_residual, law_parse and
; law_format. A list per law was three lists that drifted, so an input added
; for one was not seen by the others.

(library
 (name inputs))
